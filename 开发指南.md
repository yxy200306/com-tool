# COM-Tool 开发指南（代码框架与扩展说明）

面向"接手二次开发"的工程师。读完本文你应能在 30 分钟内定位任意功能的代码位置，并按"扩展点"小节新增功能。

---

## 1. 技术栈与为什么这么选

| 关注点 | 选型 | 原因 |
|--------|------|------|
| 通信 | Node `serialport` + `dgram` | 串口与 UDP 网络收发；Windows 下串口用 N-API 预编译 |
| 界面 | Electron（Chromium 渲染） | 桌面体验；**且渲染进程是浏览器，可直接加载并调用 HTML 协议工具** |
| 协议工具集成 | `<iframe srcdoc>` | srcdoc iframe 与主页面**同源**，可完全脚本化驱动任意自包含 HTML 工具 |

一句话：**主进程管硬件，渲染进程管界面与协议解析**，两者通过 IPC 通信。

---

## 2. 进程模型与目录

```
com_tool/
├─ main.js          【主进程】窗口创建 + 串口/UDP/文件的所有 IPC 实现（唯一能碰硬件/磁盘的地方）
├─ preload.js       【桥】contextBridge 把白名单 API 暴露给渲染进程（window.serialAPI / window.dialogAPI）
├─ renderer/        【渲染进程】纯前端，无 Node 权限，只能通过 preload 暴露的 API 与主进程通信
│   ├─ index.html   三栏布局骨架（左=串口设置，中=收发，右=协议解析）
│   ├─ style.css    全部样式
│   ├─ app.js       业务主逻辑：收发、分包、选择、统计、自动解析+波特率自适应编排
│   └─ protocol.js  协议工具加载引擎（独立模块，暴露全局 Protocol 对象）
├─ package.json     依赖 + electron-builder 打包配置（build 段）
├─ .npmrc           国内镜像（registry + electron 二进制）
├─ release/         打包产物（exe）—— electron-builder 生成
└─ node_modules/
```

**安全边界**：渲染进程 `contextIsolation:true / nodeIntegration:false`，拿不到 `require`/`fs`。
任何"读写文件、开串口"的新能力，**必须**在 main.js 加 IPC，再在 preload.js 开一个口子。

---

## 3. 数据流（最重要，先理解这张图）

```
                 ┌──────────────── 主进程 main.js ────────────────┐
 物理串口 ──data──►  port.on('data') ──IPC 'serial:data'──┐         │
                 └─────────────────────────────────────────┼────────┘
                                                            ▼
        ┌──────────────────── 渲染进程 app.js ──────────────────────────┐
        │ onSerialData(bytes)                                            │
        │   累积到 rxAccum，重置 gap 定时器(默认30ms)                     │
        │ flushRx()  ── 间隔超时 → 一"包" ──► addPacket('rx', bytes)     │
        │                                        │                       │
        │   packets[] (内存帧表)  ◄──────────────┘                       │
        │   renderPacket() → 渲染成 #rx-list 里一行 .pkt（可点选）        │
        │                                                                │
        │   若勾"自动解析" → autoParsePacket(pkt)                         │
        │        │                                                       │
        │        ├─► Protocol.parseHex(hex) ──► 驱动协议工具 iframe       │
        │        │        return {html, valid, ...}                      │
        │        ├─► Protocol.showResult(html) → 右侧结果窗口             │
        │        └─► 若 !valid 且勾"波特率自适应" → baudScan()            │
        └────────────────────────────────────────────────────────────────┘

 手动解析：选中若干 .pkt → 点"解析选中" → selectedBytes()按时间顺序拼接 → 同上 parseHex
```

**关键点**：报文以"包对象"形式存在 `packets[]`，UI 只是它的投影。
"被时间戳截断的多包一起解析"靠的是 `selectedBytes()` 把多个选中包的 `bytes` 顺序拼接。

---

## 4. 关键数据结构

### 包对象（app.js 全局 `packets[]`）
```js
{ id: Number,        // 自增唯一 id（选择/渲染用）
  time: Date,        // 收/发时刻
  dir: 'rx' | 'tx',  // 方向
  bytes: number[] }  // 原始字节（0~255）
```

### Protocol 引擎 API（protocol.js 暴露的全局 `Protocol`）
```js
Protocol.init()                  // 启动时调用一次，绑定 iframe
Protocol.load(filePath) -> {name}// 载入协议工具 HTML，识别其输入/解析/结果区
Protocol.parseHex(hex)           // 驱动工具解析一帧，返回 {ok, html, text, valid, reason}
Protocol.showResult(html, meta)  // 把结果(带原生样式)追加到右侧结果窗口
Protocol.clearResults()
Protocol.isLoaded() -> bool
```

---

## 5. IPC 接口清单（main.js ↔ preload.js ↔ 渲染）

| 渲染调用 | 主进程 handler | 作用 |
|----------|----------------|------|
| `serialAPI.list()` | `serial:list` | 枚举串口 |
| `serialAPI.open(opts)` | `serial:open` | 打开（path/baud/data/stop/parity/rtscts） |
| `serialAPI.close()` | `serial:close` | 关闭 |
| `serialAPI.setBaud(n)` | `serial:setBaud` | **热改波特率**（波特率自适应用） |
| `serialAPI.write(bytes)` | `serial:write` | 发送字节数组 |
| `serialAPI.setSignals(sig)` | `serial:setSignals` | 设 DTR/RTS |
| `serialAPI.onData/onError/onClosed(cb)` | 主进程 `send` | 串口→渲染的事件推送 |
| `networkAPI.listAddresses()` | `network:listAddresses` | 枚举本机可绑定 IPv4 地址 |
| `networkAPI.open/close(opts)` | `network:open/close` | 打开/关闭 UDP 本地监听 |
| `networkAPI.write(bytes, target)` | `network:write` | 向远端 IPv4 地址和端口发送 UDP 数据 |
| `networkAPI.onData/onError/onClosed(cb)` | 主进程 `send` | UDP→渲染的事件推送 |
| `dialogAPI.openProtocol()` | `dialog:openProtocol` | 选协议工具 HTML |
| `dialogAPI.readProtocol(p)` | `protocol:read` | 读其文本（用于 srcdoc 注入） |
| `dialogAPI.saveLog(text)` | `dialog:saveLog` | 保存接收数据 |
| `dialogAPI.openSendFile()` | `dialog:openSendFile` | 读取要发送的文件 |

---

## 6. 协议工具"接口约定"（适配新工具的核心）

任何自包含 HTML 页面，只要满足三点即可被加载：

1. **输入框**：填 HEX 报文。识别顺序 `#parse-input` → 任意 `textarea` → `input[type=text]`。
2. **解析触发**：优先调用全局函数 `parseFrame`/`parse`/`doParse`/`onParse`/`decode`；
   找不到则点击"文字含 解析/parse/decode/分析 的按钮"。
3. **结果容器**：识别顺序 `#parse-result` → `[id*=result]` → `.result-box`，解析后其 innerHTML 即结果。

识别逻辑全部在 `protocol.js` 的 `detectApi(win, doc)`，要支持新约定改这里即可。

**结果渲染**：右侧其实是两个 iframe——
- `#parser-frame`（隐藏）：真正运行协议工具、产出结果 HTML；
- `#result-frame`（可见）：把抓到的结果 + 工具的 `<style>` 重新拼成 srcdoc 展示，**保留原生配色/排版**，并保留历史（`resultBlocks[]`）。

---

## 7. 扩展点（照着做就能加功能）

### ➤ 加一个串口参数（示例：自定义 break/校验细项）
1. `index.html` 左栏 `串口设置` 加一个控件，给 id。
2. `app.js / openPort()` 把它读进 `opts`。
3. `main.js / serial:open` 把字段传给 `new SerialPort({...})`。

### ➤ 加一个接收显示模式（示例：HEX+ASCII 并排）
只改渲染层：`app.js / packetText(pkt)`——它把一个包格式化成一行 HTML。
新增一个开关 checkbox，在这里分支返回不同格式，再 `rerenderAll()`。

### ➤ 加一种发送方式（示例：周期递增帧）
参考 `doSend()`：拿输入→转字节→`serialAPI.write(bytes)`→`addPacket('tx', bytes)` 更新统计。
定时类参考 `startTimedSend()`（`setInterval`）；多条参考 `runMultiSequence()`。

### ➤ 适配一个新协议工具的特殊约定
改 `protocol.js / detectApi()`，加上你工具的选择器/函数名即可，其余流程不变。

### ➤ 调整"乱码/正确"判定（影响波特率自适应灵敏度）
改 `protocol.js / evaluate(html, text)`：
- `errMarkers`：命中即判为乱码（如 `crc-fail`、`错误`、`✗`）；
- `okMarkers` / 长度阈值：判为正确。
新协议若有别的成功/失败特征词，在这两处增减。

### ➤ 调整波特率自适应策略
`app.js / baudScan()`：候选列表来自界面输入框（`getCandidates()`）；
每个波特率 `setBaud` → `collectDuring(dwell)` 抓最长的新包 → `parseHex` 判定。
要改"每档停留时长"调 `dwell`；要改"挑哪一包"调 `collectDuring`。

### ➤ 加一个需要碰硬件/磁盘的新能力
固定三步：① `main.js` 写 `ipcMain.handle('xxx:yyy', ...)`；
② `preload.js` 在 `serialAPI`/`dialogAPI` 暴露一个调用它的方法；
③ 渲染层调用。**不要**试图在渲染层直接 `require('fs')`，拿不到。

---

## 8. 构建与打包

```powershell
npm install            # 装依赖（.npmrc 已配国内镜像）
npm start              # 开发运行
npm run dist           # 打包：release/ 下生成 portable 单文件 exe + NSIS 安装包
npm run dist:portable  # 只出免安装单文件 exe
npm run pack           # 只解包到 release/win-unpacked（调试打包用，不生成 exe）
```

产物（`release/`）：
- `COM-Tool-V2.1-portable.exe` —— **免安装单文件**，双击即用
- `COM-Tool-Setup-2.1.0.exe` —— NSIS 安装程序

打包配置在 `package.json` 的 `build` 段（appId、productName、win 目标、artifactName 等）。

**关键：winCodeSign 解压报错已自动修复**
electron-builder 的 `winCodeSign-2.6.0.7z` 里含 macOS 符号链接，Windows 普通用户
（无管理员/未开开发者模式）解压会报 *Cannot create symbolic link：客户端没有所需的特权*
导致打包中止。本项目用 `scripts/prepare-build-cache.js` 解决：先下载该包，再用 7za
**排除 darwin 目录**解压到稳定缓存名 `winCodeSign-2.6.0`，electron-builder 见缓存存在即
跳过解压。该脚本已挂在 `predist`，**`npm run dist` 会自动执行，无需手动干预、无需管理员权限**。
（如需单独跑：`npm run prepare-cache`。）

**其它常见坑**
- *Electron 二进制下载 ECONNRESET*：国内网络问题，`.npmrc` 已指向 npmmirror；
  仍失败就手动 `$env:ELECTRON_MIRROR="https://registry.npmmirror.com/-/binary/electron/"; node node_modules/electron/install.js`。
- *electron-builder 下载 nsis 等失败*：`predist` 脚本已默认带 npmmirror 镜像；
  也可显式 `$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"`。
- *"Access is denied / 拒绝访问" 删除 win-unpacked*：有 COM-Tool.exe 还开着，
  先关掉所有 COM-Tool/electron 进程再打包。
- *原生模块*：serialport 用 N-API 预编译（ABI 稳定），Electron 下直接可用，一般无需 `electron-rebuild`。
  `build.asarUnpack: ["**/*.node"]` 已保证原生库被解出 asar，可正常加载。

---

## 9. 一眼定位表

| 我想改… | 去这里 |
|---------|--------|
| 串口开/关/读/写/改波特率 | `main.js` 对应 `ipcMain.handle` |
| 暴露给前端的 API | `preload.js` |
| 界面长相/布局 | `renderer/index.html` + `style.css` |
| 收发/分包/选择/统计 | `renderer/app.js` |
| 自动解析、波特率自适应编排 | `renderer/app.js`（`autoParsePacket` / `baudScan`） |
| 协议工具识别与结果渲染 | `renderer/protocol.js` |
| 打包参数 | `package.json` 的 `build` 段 |
```
